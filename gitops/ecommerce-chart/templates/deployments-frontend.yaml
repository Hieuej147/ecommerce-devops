################################ FRONTEND DEPLOYMENTS ################################

#================== 1. Customer Storefront ==================#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront
  namespace: {{ .Values.namespace }}
  labels:
    app: storefront
spec:
  replicas: {{ .Values.storefront.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: storefront
  template:
    metadata:
      labels:
        app: storefront
    spec:
      terminationGracePeriodSeconds: 10
      containers:
        - name: storefront
          image: "{{ .Values.storefront.image }}:{{ .Values.storefront.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.storefront.port }}
          env:
            - name: PORT
              value: "{{ .Values.storefront.port }}"
            - name: NODE_ENV
              value: "production"
            - name: BACKEND_API_URL
              value: "http://api-gateway:{{ .Values.apiGateway.port }}"
            - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: CLERK_PUBLISHABLE_KEY
            - name: NEXT_PUBLIC_ADMIN_DASHBOARD_URL
              value: "https://admin.{{ .Values.domain }}"
          resources:
            {{- toYaml .Values.storefront.resources | nindent 12 }}

---
#================== 2. Admin Dashboard ==================#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-dashboard
  namespace: {{ .Values.namespace }}
  labels:
    app: admin-dashboard
spec:
  replicas: {{ .Values.adminDashboard.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: admin-dashboard
  template:
    metadata:
      labels:
        app: admin-dashboard
    spec:
      terminationGracePeriodSeconds: 10
      containers:
        - name: admin-dashboard
          image: "{{ .Values.adminDashboard.image }}:{{ .Values.adminDashboard.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.adminDashboard.port }}
          resources:
            {{- toYaml .Values.adminDashboard.resources | nindent 12 }}
